// Practice Task 1

var oranges = 350;
var apples = 350;
var total = (oranges + apples);
  console.log(total);

    var total = 1000;
    var cost = 700;
    var return1 = (total - cost);
      console.log(return1);

    // The Shopkeeper is return 300/= (solved)